export function professionalJobPath(jobId: string): `/professional/jobs/${string}` {
  return `/professional/jobs/${jobId}`;
}

export function professionalApplicationPath(
  applicationId: string,
): `/professional/applications/${string}` {
  return `/professional/applications/${applicationId}`;
}

export function facilityJobPath(jobId: string): `/facility/jobs/${string}` {
  return `/facility/jobs/${jobId}`;
}

export function facilityApplicationPath(
  applicationId: string,
): `/facility/applications/${string}` {
  return `/facility/applications/${applicationId}`;
}

export function adminVerificationPath(
  verificationId: string,
): `/admin/verification/${string}` {
  return `/admin/verification/${verificationId}`;
}
