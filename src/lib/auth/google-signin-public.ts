/**
 * Public Google login CTA. Keep the OAuth route and button implementation,
 * but do not offer Google sign-in until the Cloud redirect URI is verified.
 * Flip to true after that check.
 */
export const GOOGLE_SIGNIN_PUBLIC = false;
